import { ContainRefDirective } from './contain-ref.directive';

describe('ContainRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainRefDirective();
    expect(directive).toBeTruthy();
  });
});
